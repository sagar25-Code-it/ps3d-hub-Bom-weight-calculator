{
  "name": "ps3d-hub-engineering-workbench",
  "version": "2.0.1",
  "description": "Traceable theoretical mass calculations and a local-first engineering BOM workspace.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": "PS3D Hub",
  "scripts": {
    "build": "node scripts/build-public.mjs",
    "check:deployment": "node scripts/check-deployment.mjs",
    "dev": "node scripts/server.mjs",
    "preflight": "npm run build && npm run check && npm test",
    "test": "node --test",
    "check": "node --check js/app.js && node --check js/engine.js && node --check js/export.js && node --check js/materials.js && node --check js/shapes.js && node --check js/storage.js && node --check scripts/build-public.mjs && npm run verify:deploy",
    "verify:deploy": "node scripts/build-public.mjs --check"
  }
}
